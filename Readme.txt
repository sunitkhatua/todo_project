1) Open the file in the compiler(ie. Vs Code, Pycharm).
2) Open view.py, url.py(apps)
3) Open the terminal and go to the file location of the project.
4) In the terminal write 'python manage.py runserver' then click enter and click on the link in the terminal).
5) Use 'sunitkhatua2580' as username and 'Sunit@123' as password for Django Admin.

If you want to change database then you can modify it from 'models.py'.
